"""harness-lab — the instrument that measures harness envelopes against each other.

This package holds INSTRUMENT code (runner, graders, analysis), which is gated by
the harness. Trial workspaces are never gated — see docs/SPEC.md D8.
"""

__version__ = "0.1.0"
